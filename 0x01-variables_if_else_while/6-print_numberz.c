#include<stdio.h>

/**
 * main - the entry point of the program
 * Return: Always 0(success)
 */

int main(void)
{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar("\n");

	return (0);
}
